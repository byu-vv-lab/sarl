/**
 * The public interface package dealing with symbolic expressions.
 * 
 * {@link edu.udel.cis.vsl.sarl.IF.expr.SymbolicExpression}
 * defines the interface for a symbolic expression,
 * the root of the symbolic expression type hierarchy.  Two sub-types
 * are {@link BooleanExpression} (used for all expressions of boolean type)
 * and {@link NumericExpression} (used for all expressions with a numeric
 * type).
 */
package edu.udel.cis.vsl.sarl.IF.expr;